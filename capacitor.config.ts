import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pwa-native-indexed',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
